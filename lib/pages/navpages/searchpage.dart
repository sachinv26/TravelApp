import 'package:flutter/material.dart';
class SeacrhPage extends StatelessWidget {
  const SeacrhPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('SearcH Page')),
    );
  }
}
